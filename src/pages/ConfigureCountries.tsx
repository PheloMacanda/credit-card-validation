import { country_list, banned_country_list } from "../constants/countries";

const ConfigureCountries = () => {

    return (
        <p>Add or Remove Banned Countries</p>
    )
}

export default ConfigureCountries;